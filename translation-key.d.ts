/**
 * Tag stand-alone translation keys with this tagged template literal to have them discovered by `ember-intl-analyzer`.
 * @param strings : The translation key to tag.
 */
export function translationKey(strings: TemplateStringsArray): string;
